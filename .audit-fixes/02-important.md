# Important fixes (0 / 19)

Expected by serious adopters.

---

## [x] I1 — otp HTTP handlers (enumeration-safe) — DONE
**Where:** new `otp/handlers.go`. **Problem:** otp imports no net/http; every adopter must reimplement enum-safe error collapsing. **Fix:** issue/verify `http.HandlerFunc` factories mirroring identity/mfa; collapse `ErrInvalidCode`/`ErrCodeNotFound`/`ErrTooManyAttempts` into one response. **Test:** handler tests for collapse + status mapping.

## [x] I2 — CSRF on tokens RefreshHandler / LogoutHandler — DONE
**Where:** `tokens/handlers.go`. DONE: `WithTrustedOrigins` + Origin/Referer check → 403. NOTE: shared double-submit-cookie helper not shipped (origin allowlist used instead). **Problem:** cookie-auth state-changing POSTs have no origin check (not even opt-in); identity is opt-in only. **Fix:** add `WithTrustedOrigins`/shared CSRF middleware to token handlers; consider a reusable double-submit-cookie helper. **Test:** cross-origin POST rejected when origins configured.

## [x] I3 — RequestEmailVerification enumeration safety — DONE (commit 6076b2f)
**Where:** `identity/service.go:300-302`. **Problem:** returns store error (500) vs uniform 204 — leaks live/same-tenant userID. **Fix:** swallow ErrUserNotFound at service / always 204 at handler (match reset/magic-link). **Test (confirm-first):** unknown/foreign userID currently 500; assert 204.

## [x] I4 — Email validation + normalization — DONE
**Where:** `identity` Register/LinkOrCreateIdentity + handlers. **Problem:** verbatim email (only TrimSpace); byte-exact uniqueness → `User@x.com`≠`user@x.com`; empty/malformed registers. **Fix:** `net/mail.ParseAddress` + trim+NFC+lowercase; enforce uniqueness on normalized form. **Test (confirm-first):** case-variant dup registers today; assert rejected/normalized.

## [x] I5 — Authenticated change-email flow (verify-before-switch) — DONE
**Where:** `identity/service.go` + handlers + Store. DONE: `RequestEmailChange`/`ConfirmEmailChange` on Service (kind `email_change`, new email carried as token metadata, delivered to the NEW address via new `Mailer.SendEmailChange`); `RequestEmailChangeHandler`/`ConfirmEmailChangeHandler` + `WithEmailChangeField`. Atomicity: new `Store.UpdateUserEmail` swaps `users.email`+`email_verified_at` AND re-keys the password identity's `provider_id` (password identities are keyed by email) in one atomic op — single CTE on pgx, locked multi-update on memory — so a uniqueness conflict on either index aborts the change. Confirming (token went to the new address) marks the new address verified. **Problem:** no ChangeEmail; only internal Store.UpdateUser. **Fix:** request → token to NEW address → confirm → atomic swap + reset EmailVerifiedAt. **Test:** swap-only-after-confirm, normalize, taken-up-front + claimed-in-interim, single-use, expiry, kind isolation, deactivated-account, unknown-user; handler 401/delivery/error-mapping; cross-backend `Contract: Change Email` (memory + pgx).

## [x] I6 — User-facing account deletion / deactivation (+GDPR) — DONE (DeleteAccount; DeactivateAccount deferred)
**Where:** `identity/service.go` + handlers. DONE: `Service.DeleteAccount(ctx, userID, opts)` runs a consumer-wired cross-module revocation seam (`AccountEraser` hooks via `WithAccountErasers` — sessions/refresh-families/MFA/passkeys) then soft-deletes+anonymizes the identity (frees the email slot, erases PII). Erasers run FIRST and `errors.Join`; any failure aborts before soft-delete so the op is cleanly retriable. `DeleteAccountHandler` (auth-gated via WithUserResolver, POST-only, origin check) clears the auth cookies on success. Review-driven hardening to `Store.DeleteUser`: now also **purges the user's pending verification tokens** (GDPR — they carried user_id + plaintext change-email PII), pgx made **atomic** (single CTE for users+identities+tokens), and pgx now returns `ErrUserNotFound` on no-match (was a silent no-op, diverging from memory). Step-up gate deferred to I15. Modules stay decoupled (identity never imports sessions/tokens/mfa/passkey), matching the ChangePassword "cross-module revocation is the consumer's job" precedent — the seam makes it first-class. **Problem:** DeleteUser is Store-only, internal orphan-comp only. **Fix:** DeleteAccount/DeactivateAccount on Service + handler; cascade revoke; consider hard-erase + data-export. **Test:** soft-delete+anonymize+email-slot-freed, erasers-run-with-userID, eraser-failure-aborts-before-delete (retry-safe), unknown-user-skips-erasers, double-delete→NotFound; handler 401/405/cookie-clear/error-mapping.
> Deferred (not blockers, noted for later): **DeactivateAccount** (reversible suspension) needs a new account-status column + migration + store changes across backends — distinct from soft-delete-with-anonymization; **hard-erase + data-export** are "consider" extras. Track as a follow-up if a reversible-suspend or GDPR-export requirement lands.

## [x] I7 — Breach-password check: ship HIBP k-anonymity impl — DONE
**Where:** new `passwords/breach/hibp` + `passwords/breach/offline`. DONE: `hibp.Client` implements `passwords.BreachChecker` via the HIBP range API — SHA-1s the password and sends ONLY the first 5 hex chars (`/range/{prefix}`), resolving the 35-char suffix locally; opts `WithHTTPClient`/`WithBaseURL`/`WithUserAgent`/`WithThreshold`/`WithAddPadding`/`WithFailOpen`. Default fails CLOSED (error propagates through `PassphrasePolicy.Verify`); `WithFailOpen` returns (false,nil) on upstream failure. Zero-count padding entries never match. Plus `offline.Checker` (no network, air-gapped/fail-safe) loaded from SHA-1 hashes (`<hash>` or HIBP `<hash>:<count>`, threshold-aware) or plaintext lists. **Problem:** `BreachChecker` is interface-only; no client ships. **Fix:** HIBP range API (only 5 prefix chars leave), documented fail-open vs fail-closed; optional offline blocklist loader. **Test:** asserts only-prefix-sent + suffix-never-leaves, case-insensitivity, padding ignored, threshold boundary, fail-open vs fail-closed, ctx cancellation; offline plaintext/hash/threshold/malformed. (httptest, no real network.)

## [x] I8 — Email/SMS delivery implementation + templating — DONE
**Where:** new `delivery/` package. DONE: reference `SMTPMailer` (stdlib `net/smtp`, zero new deps) implementing `identity.Mailer` for all four flows — STARTTLS (default, **fails closed** if the server doesn't advertise it, never sending creds/PII in cleartext), implicit TLS (465) and a TLSNone mode for local/test relays; AUTH PLAIN; RFC5322/CRLF composition with RFC2047-encoded subject (newlines collapsed to block header injection) and quoted-printable bodies, emitting `multipart/alternative` when an HTML body is present; context-honouring dial + conversation (deadline + `AfterFunc` teardown). Templating hook: `Renderer` interface + default `TemplateRenderer` (text/template subject+text, **html/template** HTML so user data is auto-escaped) with `WithTemplate` per-event overrides and built-in provider-neutral copy; `LinkConfig`/`LinkBuilder` turn a token into the action link (email-change also carries the new address). OTP seam: `Sender` (email/SMS channel), `Contact` + `ContactResolver`/`ContactResolverFunc` (maps the opaque subject UUID → email/phone), and `OTPDelivery(resolver, WithEmailSender/WithSMSSender/WithOTPMessage)` building the `func(ctx,*otp.Challenge) error` `otp.IssueHandler` expects (prefers email, falls back to SMS, errors on no channel). Fail-fast: `NewSMTPMailer` rejects empty host/From, unparseable From, and (review-driven fix) an **all-empty `LinkConfig`** for the default templates; `NewTemplateRenderer`/`OTPDelivery` validate at construction. SMS story documented in `doc.go`: no SMS Sender ships (every provider is a paid vendor SDK) — implement the one-method `Sender` seam (Twilio/SNS/etc.); mfa still excludes SMS as a factor (NIST), and a full phone-verification flow is the N9 follow-up. **Test:** 41 tests — template render/override/escaping/parse-fail/subject-collapse/link-builder; SMTPMailer against an in-process fake SMTP (headers/recipient/body link, email-change→new address, multipart, STARTTLS-unsupported fail-closed, ctx-cancelled, invalid recipient, all-empty-LinkConfig rejected); OTP routing (email/SMS/prefer-email/fallback/no-channel/resolver+sender errors/custom message).

## [x] I9 — OAuth OIDC id_token / nonce / JWKS validation — DONE
**Where:** `oauth/` (new `oidc.go`; `provider.go`/`handlers.go`/`state.go`/`google.go` touched). DONE: opt-in `WithOIDC(OIDCConfig{Issuer, JWKSURL, Audience?, AllowedAlgs?, Leeway?, ClaimsMapper?, HTTPClient?})`. When enabled, `Exchange` reads the `id_token` from the token response and validates it via golang-jwt/v5 (`jwt.Parse` with `WithValidMethods` [RS/ES 256/384/512 only — "none"/HMAC rejected = alg-confusion defense], `WithIssuer` exact, `WithAudience`, `WithExpirationRequired`, `WithIssuedAt`, `WithLeeway` 60s), then derives `UserInfo` from the verified claims (sub→ProviderID, email, email_verified [bool or "true"], name) instead of an access-token userinfo GET. Signature verified against the issuer's JWKS via a dependency-free `jwksCache` (parses RSA n/e and EC x/y; EC built with `ecdsa.ParseUncompressedPublicKey` so the point is curve-validated and no deprecated raw-coord fields; skips `use:"enc"`; refetches on kid-miss/TTL; honours ctx). **Nonce** minted in `BeginHandler` for OIDC providers, sent on `/authorize` (`WithAuthNonce`), bound through the state cookie (now `state.verifier.nonce`, 2-part legacy tolerated) and verified mandatorily + constant-time on the id_token at callback (empty/absent = fail). **Review-driven hardening:** OIDC Core 3.1.3.7 azp check — a present `azp` must equal the audience, and a multi-audience id_token must carry such an azp (closes the confused-deputy gap that `WithAudience`'s containment check leaves open; matters for shared-issuer / per-tenant SSO). `WithOIDC` fails fast (panics) on empty Issuer/JWKSURL/unresolvable audience. `AuthCodeURL`/`Exchange` made variadic — backward compatible; GitHub/Discord (non-OIDC) unaffected. `GoogleIssuer`/`GoogleJWKSURL` exported for `oauth.Google(..., WithOIDC(...))`. **Test:** 40 oauth tests incl. valid; bad sig/iss/aud/expired/nonce-mismatch/missing-nonce/empty-expected-nonce/missing-sub/unknown-kid; alg=none + HS256-confusion rejected; multi-aud-without-azp + azp-mismatch rejected, multi-aud+matching-azp accepted; ES256 end-to-end; JWKS parse RSA+EC (+enc skipped, no-usable-keys); config validation; WithOIDC panic; handler begin-mints-nonce / non-OIDC-no-nonce / callback success / nonce-mismatch / missing-id_token. **Follow-on:** generic OIDC discovery (`.well-known`) for per-tenant provider assignment — see [feature-per-tenant-oidc.md](feature-per-tenant-oidc.md).

## [x] I10 — JWT signing-key rotation (kid / keyset / overlap) — DONE
**Where:** `tokens/jwt/issuer.go`. DONE: `Config.SigningKeys []SigningKey{KeyID,Secret}` + `ActiveKeyID` — a kid-tagged HS256 keyset. The active key signs new access tokens (stamping its `KeyID` as the `kid` header) while EVERY key in the set verifies, so a token signed by a now-retired key keeps validating until it expires (overlapping-validity rollover): deploy the set with the new key added + ActiveKeyID switched, then drop the old key once its longest-lived token has expired. `resolveKeyset` builds the signing key/kid + a `verifyKeys` map + a `legacyKey`; `New` panics (fail-fast) when no coherent signer can be built (no key, missing/duplicate KeyID, unresolvable ActiveKeyID); `Config.Validate` reports keyset problems (KeyID present/unique, Secret ≥32B, ActiveKeyID resolvable) joined. **Backward compatible:** `SecretKey`-only callers are unchanged — tokens stay kid-less and verify via the legacy path; `SecretKey` may be kept alongside `SigningKeys` as the legacy verifier so kid-less tokens minted before rotation keep working through the migration window. Verification pins HS256, selects the key by `kid`, and rejects an unknown kid. **Review-driven hardening:** a PRESENT but malformed `kid` (non-string, e.g. a JSON number, or empty string) is now rejected rather than silently treated as kid-less and verified against the legacy key — closing a contract-conformance / log-integrity gap (a non-string kid could otherwise hide legacy-key usage from kid-based monitoring during the exact retirement window). Asymmetric/JWKS signing left as a documented future extension (symmetric keyset fully solves the rotation-breaks-live-tokens problem). **Test:** kid stamped with active key; retired key verifies during overlap then rejected once dropped; unknown kid rejected; legacy kid-less token verifies after enabling rotation; kid-less rejected without a legacy key; Rotate continuity across a key change; malformed-kid (numeric/empty) rejected; New panics on malformed keysets; Validate keyset cases. (39 jwt tests; tokens suite 92 incl. pgx.)

## [x] I11 — Audit logging / security-event hooks / slog seam — DONE
**Where:** new `event/` package + wired into identity/mfa/tokens-jwt services and identity handlers. DONE: dependency-free `event` seam — `Event{Type,TenantID,UserID,Reason,Err,Attrs}`, a `Sink` interface, `SinkFunc`, nil-safe `Emit`, `MultiSink`, and `NewSlogSink` (the injectable `*slog.Logger` seam: Err→Error, failure/anomaly types→Warn, else Info). Wired the audit's named events: **identity.Service** (`WithEventSink`) emits LoginSucceeded / LoginFailed (uniform "invalid_credentials", no userID on enumeration-safe unknown-account paths) / AccountLocked, plus UserRegistered, PasswordReset, PasswordChanged, EmailVerified, EmailChanged, MagicLinkLogin, AccountDeleted — all emit-only-on-success. **tokens/jwt** (`Config.EventSink`) emits RefreshReuseDetected + TokenFamilyRevoked from Rotate. **mfa.Service** (`WithEventSink`) emits MFAEnrolled/MFAConfirmed/MFAVerificationFailed/MFADisabled. **Swallowed-error visibility:** identity handlers (`WithHandlerEventSink`) emit `DeliveryFailed` (with the underlying error) when the Mailer fails on the enumeration-safe Request* flows — the failure is dispatched off the response path so the uniform response is preserved while the outage becomes observable. Events are an internal observability channel (never client-visible) carrying no secrets/PII/raw input, so emitting on failed-login paths is not an enumeration oracle. **Review-driven fix:** AccountLocked is now gated on `IncrementFailedAttempts` succeeding (was emitted from a pre-increment prediction even when the store errored and no lock persisted — a misleading audit record). **Test:** event pkg (nil-safe Emit, MultiSink fan-out, SinkFunc, SlogSink level/attr mapping); identity (register+login success carries userID, failure+lockout threshold, unknown-account carries no userID, increment-error→no AccountLocked, handler DeliveryFailed via failing mailer); mfa lifecycle; jwt strict-mode reuse→both events. (193 tests across the 4 pkgs; full module green incl. pgx.)
> Follow-up (not blocking): sessions/otp/passkey/oauth services don't yet emit; they can adopt the same `event.Sink` seam (the `event` package is ready). Wire as those modules gain audit requirements.

## [x] I12 — Sessions refresh / sliding expiry / rotation + fixation — DONE
**Where:** `sessions/service.go` + Store (memory + pgx + contract). DONE: `Touch(ctx, token, duration, opts)` slides a session's expiry to now+duration without changing its token (idle-timeout primitive), and `Rotate(ctx, token, duration, opts)` issues a fresh token for the SAME logical session (same ID + metadata), invalidating the old token and resetting the lifetime — the session-fixation defense to call after any privilege change (login over an anonymous session, MFA/step-up, role grant). Both reject an unknown/expired session with `ErrSessionNotFound` (they go through ValidateSession). Backed by a new `Store.UpdateSession`, implemented as a **compare-and-set on the token hash** (review-driven): the swap matches `id AND tenant AND token_hash == expected`, so two requests racing to rotate the same token can't both "succeed" — the first swaps, the loser gets an honest `ErrSessionNotFound` instead of a fresh token that would never validate (mirrors the refresh-token reuse philosophy). The client-facing opaque token is what rotates (the internal row UUID stays, since it isn't the credential). Cross-backend: memory + pgx implementations + a `StoreContractTesting` "Update Session (Touch/Rotate)" case (token swap, expiry extend, stale-CAS rejected, unknown→not-found) that runs against real postgres. **Test:** Touch extends expiry + same token still validates; Rotate changes token + old token invalidated + same session ID + user/tenant preserved; Touch/Rotate reject expired + unknown; concurrent-rotate loser gets ErrSessionNotFound (mock CAS-fail). (30 sessions tests incl. pgx.)

## [x] I13 — Passkey discoverable / usernameless login — DONE
**Where:** `passkey/service.go`. DONE: `BeginDiscoverableLogin()` (wraps go-webauthn's discoverable API — empty allowCredentials, resident-key flow, no userID needed) and `FinishDiscoverableLogin(ctx, session, r, opts...)` which resolves the account from the assertion's user handle (the account UUID libauth sets as the WebAuthn ID) via a `DiscoverableUserHandler`, verifies the assertion against that account's credentials, rejects a regressed counter (ErrCredentialCloned), persists the updated credential, and returns `(*Credential, resolvedUserID, error)`. Multi-tenant note documented: the handle is global but credential lookup stays tenant-scoped, so pass `WithTenant` derived from the request. Review: no findings (go-webauthn enforces the handle→user binding + credential ownership; uuid.FromBytes guards the handle length). **Test:** built a reusable software WebAuthn authenticator harness (`authenticator_test.go` — ES256/P-256, "none" attestation, real attestation+assertion responses) used to drive the FULL Finish ceremonies end-to-end; discoverable tests assert begin has no allowCredentials + no bound user, finish resolves the user from the user handle without a prior username, sign-count persisted, and an unknown handle is rejected. (17 passkey tests; the harness is shared with I14.)

## [x] I14 — Passkey Finish-ceremony end-to-end test — DONE
**Where:** `passkey/ceremony_test.go` (+ the `authenticator_test.go` harness shipped with I13). DONE: the software WebAuthn authenticator (ES256/P-256, "none" attestation, real attestation+assertion responses, signature over authData‖SHA256(clientDataJSON)) drives the full Finish ceremonies end-to-end, exercising the previously-untested `FinishRegistration` verification + `toStored` + `SaveCredential` path, the `FinishLogin` assertion-verification + counter-update + `UpdateCredential` path, `ErrCredentialCloned`, and sign-count persistence. **Test:** FinishRegistration stores the credential (retrievable via ListCredentials, public key persisted, counter 0); FinishLogin verifies the assertion and advances+persists the counter; sign-count advances and persists across three successive logins; **clone detection** — a second assertion with a non-advancing (regressed) counter returns ErrCredentialCloned; an assertion bound to a forged challenge is rejected. (22 passkey tests.)
> Note: `onLoginSuccess` is the handler-level `LoginSuccessFunc` (passkey/handlers.go); the service-level Finish verification path — the audit's actual gap — is now fully exercised. A cookie-ceremony handler end-to-end test could follow if the HTTP layer needs its own coverage.

## [x] I15 — Re-authentication / step-up flow (sudo mode) + gate DisableTOTP — DONE
**Where:** `tokens/` (token.go, jwt/issuer.go, middleware.go, stores) + identity/mfa docs. DONE: an OIDC `auth_time`-based freshness model. `Claims.AuthTime` records when the subject actually authenticated and is the anchor for `Claims.FreshAuth(maxAge)` (fails CLOSED on a zero/missing auth_time). The JWT issuer stamps `auth_time` (claimsWrapper, unix) and — crucially — **preserves it across refresh**: `RefreshToken.AuthTime` is persisted (memory + pgx via migration 003, NULL-safe; contract round-trips it) and carried verbatim onto every rotated descendant, so a silent auto-refresh re-evaluates AMR/scopes but never counts as a fresh authentication. Middleware `WithMaxAuthAge(d)` is the enforceable step-up gate (403 `step_up_required` when stale), applied on both the direct-verify and auto-refresh paths via `stepUpSatisfied = amrSatisfied && FreshAuth`. It works for ANY factor, so it covers OAuth-only accounts (password re-verify alone can't). **Gating wired by docs/composition** (matching the database/sql-style design): `DeleteAccount`/`DeleteAccountHandler` and `mfa.DisableTOTP`/`DisableHandler` docs now direct callers to wrap the route with `tokens.RequireAuth(..., WithMaxAuthAge(d))`. **Review-driven HIGH-severity fix:** rotation must never *manufacture* a fresh auth_time — `issuePair` now only defaults zero→now on INITIAL issuance (`initial` flag); rotating a legacy/zero-auth_time refresh token keeps auth_time zero (fails closed), closing a hole where a silent refresh of a pre-migration token would have reset freshness and bypassed the gate. **Test:** FreshAuth boundaries (window/zero-fails-closed/no-requirement); auth_time set on issue + preserved across Rotate (IssuedAt advances, auth_time doesn't); legacy-token rotation stays zero+stale (regression for the review finding); `WithMaxAuthAge` rejects stale (step_up_required) + passes fresh + disabled-by-default. (102 tokens tests incl. pgx.)
> The I6 follow-up (gate identity.DeleteAccount + cover OAuth-only accounts) is satisfied: `WithMaxAuthAge` is the factor-agnostic enforceable primitive, and the DeleteAccount docs point at it.

## [x] I16 — Background cleanup / GC of expired records — DONE
**Where:** tokens/sessions/otp Store + identity Store (interfaces + memory + pgx + contract). DONE: a schedulable reaper on each store — `tokens.Store.DeleteExpired` (refresh tokens + expired API keys; never-expiring API keys kept), `sessions.Store.DeleteExpired`, `otp.Store.DeleteExpired`, and `identity.Store.DeleteExpiredVerificationTokens` — each returning the deleted count. `WithTenant` scopes to one tenant; no tenant option sweeps ALL tenants (the maintenance-job semantics; the `expires_at < now()` predicate is tenant-independent and never touches another tenant's live rows). Both backends implement each; cross-backend `StoreContractTesting` cases assert only-expired-removed + live-kept (run against real postgres). **Review-driven precision (medium finding):** reaping a consumed refresh-token row drops the *late* theft-detection alarm (a replay of an already-EXPIRED consumed token now reports not-found instead of revoking a still-live family). Analysis: the security-meaningful detection — a replay WITHIN the token's validity — is fully preserved (the reaper only deletes past `expires_at`, by which point the token is unusable), and retaining consumed rows until the whole family expired would defeat the GC for long-lived rotating sessions. So the behavior is kept but made precise: `DeleteExpired` doc + SECURITY.md now state the tripwire spans a token's validity, and a contract case pins that a consumed-AND-expired token is reaped. **Test:** per-store only-expired/live-kept; tokens also keeps consumed-but-not-expired (reuse detection) and reaps consumed-AND-expired; identity reaps expired verification tokens (consume→NotFound) while live ones still consume. (Run-cadence documented as "periodically, e.g. hourly".) Note: optional pgx TTL/partial indexes not added — the `DeleteExpired` sweep is sufficient; partial indexes are a deployment tuning concern.

## [ ] I17 — Secret redaction on credential-bearing types
**Where:** `tokens/token.go` (TokenPair.Access/Refresh, APIKey.Token), session token. **Problem:** plain strings, no `json:"-"`/String()/LogValue masking → logging struct leaks live creds. **Fix:** redacting secret type or String()/MarshalJSON/LogValue masking + `json:"-"` where apt. **Test:** fmt/json of struct does not contain plaintext.

## [x] I18 — Config validation / fail-fast at startup — DONE (jwt) — follow-up: identity/sessions/mfa constructors
**Where:** `tokens/jwt/issuer.go` New[C] (DONE: panics on empty key + `Config.Validate`); identity/sessions/mfa NewService (still TODO — add basic validation). **Problem:** jwt.New returns no error, silently accepts empty SecretKey + zero TTLs. **Fix:** make jwt.New return error; reject empty SecretKey, non-positive AccessTTL/RefreshTTL, empty Issuer; add basic validation to other constructors. **Test (confirm-first):** empty SecretKey currently succeeds; assert error.

## [ ] I19 — Tenant enforcement consistency across backends
**Where:** memory + pgx stores (mfa/otp/passkey + others). **Problem:** pgx enforces ErrTenantRequired only on some writes; reads/deletes query tenant_id=''; memory rarely enforces; never tested. **Fix:** enforce ErrTenantRequired uniformly on all write+read+delete (or define+document empty-tenant default); add contract tests for empty-tenant path. **Test (confirm-first):** empty-tenant read on pgx silently returns empty today; assert consistent behavior.
> Also in scope (found during I5 review): identity `UpdateUserEmail` follows the same whole-file pattern — pgx returns `ErrTenantRequired` on empty tenant, memory accepts it and succeeds; the `StoreContractTesting` callers all pass `useMultiTenant=true`, so the empty-tenant path is never exercised. Pin empty-tenant behavior for the whole identity Store (incl. `UpdateUserEmail`) here rather than special-casing one method.
