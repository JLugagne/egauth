.agents
.claude
.antigravitycli/
.questions/
.scaffor/
PRD.md
.tasks/
.reviews/
docs/public
docs/.hugo_build.lock

# coverage / test artifacts
*.out
coverage.out
coverage*.out
*.coverprofile

# Go workspace: go.work is committed (it wires the multi-module monorepo for local dev/CI);
# go.work.sum is a derived lock file and is intentionally not tracked.
go.work.sum
